using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyMove : MonoBehaviour
{
    public GameObject headPtr;                        //记录头结点

 //  public float speed;
 //   public float rotateSpeed;

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }


/*    public void moveTo(Vector3 pos)
    {//移动到前一位置
            Vector3 old = this.transform.position;  //为下一节身体记录位置
            this.transform.position = pos;          //移动到pos
            if (next != null)                       //如果有下一节身体
            {
                next.GetComponent<BodyMove>().moveTo(old);//下一节身体继续移动
            }

    }
 */   

}

